<Window x:Class="Inventario_Hardware_IT.GestionMarcas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Marcas" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Catálogo de Marcas" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Nombre de la Marca:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtNombreMarca" Width="200" Height="25" VerticalContentAlignment="Center"/>
            <Button x:Name="btnGuardar" Content="Guardar" Width="100" Margin="10,0,0,0" Height="25" Click="BtnGuardar_Click" Background="#007ACC" Foreground="White"/>
        </StackPanel>

        <DataGrid x:Name="gridDatos" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True" AlternatingRowBackground="#F0F0F0"/>
    </Grid>
</Window>